apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datadog-data
  namespace: fks-development
  labels:
    app: datadog
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
